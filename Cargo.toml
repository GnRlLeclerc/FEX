[package]
name = "fex"
version = "0.1.0"
edition = "2024"

[dependencies]
slint = "1.15.1"

[build-dependencies]
slint-build = "1.15.1"

[profile.release]
opt-level = 3
codegen-units = 1
lto = true
strip = true
